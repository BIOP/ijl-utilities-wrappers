package ch.epfl.biop.fiji.imageplusutils;

import ij.IJ;
import ij.ImagePlus;
import ij.process.ImageProcessor;
import net.imglib2.img.Img;
import net.imglib2.img.display.imagej.ImageJFunctions;

import java.util.List;

/**
 * Utility methods for ImagePlus manipulation
 */

public class ImagePlusFunctions {

    /*static public ImagePlus createEmptyImage(ImagePlus imp, int width, int height) {
        imp = IJ.createImage("HyperStack", "8-bit color-mode label", width, height, imp.getNChannels(), imp.getNSlices(), imp.getNFrames());
        imp.getNChannels()
                imp.getNSlices()
                        imp.getNFrames()
        return null;
    }

    static */


}
